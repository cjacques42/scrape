
      // const $ = cheerio.load(html);
      // $.root().contents().each(function() {
      //   // console.log(this);
      //   if (this.type === 'comment') {
      //     console.log(this);
      //     $(this).remove();
      //   }
      // });
      //
      // $('script').remove();
      // $('style').remove();
      //
      // let content = $.html();
